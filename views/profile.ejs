<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
  
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="p-10 w-full h-[100%]  bg-zinc-800  ">
      <a class="text-white mb-3 text-2xl " href="/index">Create user</a>
        <h1 class="text-white text-3xl tracking-tighter mt-5 mb-5">Users Profile</h1>
        
        <div class=" flex flex-wrap gap-3 justify-between rounded-md">
          <% users.forEach(function(user){ %>
            <div class="bg-zinc-600 w-72 p-4  rounded ">
                <div class="w-full h-72 bg-transparent   rounded-md mb-3 ">
                    <img class="w-full h-full object-cover overflow-hidden rounded" src="<%=user.image%>" alt=""/>
                </div>
               <h2 class="text-xl text-white tracking-tighter mt-3 "><%=user.name%></h2>
               <h3  class="text-xl text-white tracking-tighter"><%=user.email%></h3>
              <div class="flex gap-3 justify-between mb-3 mt-5">
                <a class="text-green-500 px-3 py-1 font-semibold  text-xl " href="/edit/<%=user._id%>">Edit User</a>
                <a class="text-red-500 px-3 py-1 font-semibold   text-xl " href="/delete/<%=user._id%>">Delete User</a>
    
              </div>
           
            </div>

          <%})%>
      
        
    </div>
    </div>
</body>
</html>